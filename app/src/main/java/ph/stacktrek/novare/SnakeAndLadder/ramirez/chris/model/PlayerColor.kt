package ph.stacktrek.novare.SnakeAndLadder.ramirez.chris.model

import android.graphics.Color

enum class PlayerColor(val colorInt: Int) {
    RED(Color.RED),
    BLUE(Color.BLUE),
    GREEN(Color.GREEN),
    YELLOW(Color.YELLOW)
}